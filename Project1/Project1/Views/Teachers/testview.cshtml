@model IEnumerable<Project1.Models.Teacher>

@{
    ViewBag.Title = "testview";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>testview</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Teach_name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Teach_name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Teach_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Teach_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Teach_ID })
        </td>
    </tr>
}

</table>
